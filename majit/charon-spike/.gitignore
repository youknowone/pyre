corpus/target/
corpus/Cargo.lock
# Charon-output artifacts that may land in the corpus dir if extraction
# is run with default flags or without --dest-file pointing elsewhere.
corpus/*.ullbc
corpus/*.llbc
prototype/target/
prototype/Cargo.lock
prototype/actual/
