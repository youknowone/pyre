[package]
name = "charon_spike_prototype"
version = "0.0.0"
edition = "2024"
publish = false

[[bin]]
name = "charon-spike-lower"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Isolate from the parent workspace so the spike never participates in the
# main build.
[workspace]
