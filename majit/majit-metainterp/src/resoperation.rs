//! RPython parity module for `rpython/jit/metainterp/resoperation.py`.
//!
//! The operation IR is shared from `majit_ir`, but the upstream import path is
//! `metainterp.resoperation`.

pub use majit_ir::resoperation::*;
