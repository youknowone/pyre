# Learn more about this config here: https://pre-commit.com/

# To enable these pre-commit hooks run:
# `pipx install pre-commit` or `brew install pre-commit`
# Then in the project root directory run `pre-commit install`

repos:
  - repo: https://github.com/codespell-project/codespell
    rev: v2.4.2
    hooks:
      - id: codespell
        args: ["--toml=pyproject.toml"]
        types: [rst]
        additional_dependencies:
          - tomli

  - repo: local
    hooks:
      - id: no-line-number-citations
        name: upstream citations name a symbol, not a line number
        entry: python3 scripts/check-new-line-citations.py
        language: system
        pass_filenames: false
        # Reads the staged diff itself, so it judges what a commit ADDS. Under
        # `pre-commit run --all-files` that diff is empty and the hook is inert,
        # which is what keeps the tree's existing numbers out of scope.
        always_run: true

      - id: cargo-fmt
        name: cargo fmt --check
        # `cargo fmt --check` is the job every other CI job waits behind, so an
        # unformatted line throws away a whole run's worth of signal for
        # something a local run answers in seconds. Check-only on purpose: a
        # hook that rewrote files would change work mid-commit.
        entry: cargo fmt --all -- --check
        language: system
        types_or: [rust]
        pass_filenames: false
